import React from 'react';
import { GrPrevious, GrNext } from "react-icons/gr";

const Pagination = ({ handlePrevPage, handleNextPage, page }) => {
  return (
   <div>

    
   </div>
  );
};

export default Pagination;